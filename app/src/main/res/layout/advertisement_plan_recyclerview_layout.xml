<?xml version="1.0" encoding="utf-8"?>

<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardview"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp">

    <LinearLayout
        android:id="@+id/plan_lay"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="@drawable/plan_gradient_blue"
        android:orientation="horizontal"
        android:weightSum="3">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingLeft="15dp">

            <TextView
                android:id="@+id/tv_plan_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/lato_bold"
                android:text="Plan 1"
                android:textAllCaps="true"
                android:textColor="@color/colorwhite"
                android:textSize="20sp" />

            <TextView
                android:id="@+id/tv_plan_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@+id/tv_plan_name"
                android:fontFamily="@font/lato_regular"
                android:text="2 active posts for 1$ (user can post a total of 2 active post for a period of 1 month from the payment date) any extra active post will cost 1$"
                android:textColor="@color/colorwhite"
                android:textSize="14sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/tv_select_plan"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:fontFamily="@font/lato_bold"
            android:gravity="center"
            android:text="@string/select_pay"
            android:textColor="@color/colorwhite"
            android:textSize="15sp" />


    </LinearLayout>

</android.support.v7.widget.CardView>